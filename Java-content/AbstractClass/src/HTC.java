public class HTC extends Phone {         //this inherits the abstract class called phone
    public void showConfiguration() {   //so it uses the method in the abstract class but not creating its own method
        System.out.println("HTC edge\nAndroid version 4.5");
    }
}
